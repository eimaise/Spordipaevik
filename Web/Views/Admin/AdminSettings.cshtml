@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}
@{
    var tempval = TempData["successmessage"] as string;
}
@if (!string.IsNullOrWhiteSpace(tempval))
{
    <div id="messages" class="alert alert-success alert-dismissible" role="alert">
        <div id="messages_content">@tempval</div>
    </div>
}
<div class="row form-group">
    <div class="col-lg ">
        Seadista perioode
    </div>
    <div class="col ">
        <button class="btn btn-primary">Perioodid</button>
    </div>
</div>

<div class="row form-group">
    <div class="col-lg">
        Uuenda uue õppeaasta klassinumbreid
    </div>
    <div class="col-lg">
        <form method="post">
            <button asp-action="ChangeClassNumbers"class="btn btn-primary">Uuenda</button>
        </form>
    </div>
</div>

<div class="row form-group">
    <div class="col-lg">
        Klassid
    </div>
    <div class="col-lg">
        <a asp-action="AddClass"class="btn btn-primary">Lisa klasse</a>
        <a asp-action="SchoolClasses"class="btn btn-primary">Nimekiri</a>

    </div>
</div>
