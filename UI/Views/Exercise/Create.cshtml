@model WebApplication2.ViewModels.Exercises.AddExerciseVm
  @{
    ViewData["Title"] = "Õpilased";
}

<form asp-action="Create" method="post">
            <input type="hidden" name="Units" value="@Model.Units"/>
    @* <div asp-validation-summary="ModelOnly" class="text-danger"></div> *@

    <div class="form-group">
        <label asp-for="@Model.Name"></label>
        <input asp-for="Name" class="form-control"/>
        <span asp-validation-for="Name"></span>
    </div>
    <div class="form-group">
        <label asp-for="Comment"></label>
        <textarea asp-for="Comment" class="form-control"></textarea>
        <span asp-validation-for="Comment"></span>
    </div>
    <div class="form-group">
        @{
            var list = new SelectList(Model.Units, "Id", "Name");
            <select class="form-control"asp-for="Selected" asp-items="list">
                @* <option value=""> None</option> *@
            </select>
        }

    </div>
    <button class="btn btn-primary"type="submit"> Lisa harjutus</button>
</form>